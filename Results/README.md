# Results folders

Each activity type has its own subfolder and dashboard tab.

- `KYC/` — KYC face-identification activity exports. Add one CSV per activity.

Files placed directly in `Results/` are intentionally ignored. When another activity type is added, give it a separate subfolder and configure its own parsing and presentation in `app.py`.
